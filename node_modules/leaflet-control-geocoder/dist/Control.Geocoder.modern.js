import * as e from "leaflet";
//#region \0rolldown/runtime.js
var t = Object.defineProperty, n = (e, n) => {
	let r = {};
	for (var i in e) t(r, i, {
		get: e[i],
		enumerable: !0
	});
	return n || t(r, Symbol.toStringTag, { value: "Module" }), r;
}, r = class extends Error {
	constructor(e) {
		super(e), this.name = "SuggestUnsupportedError";
	}
};
function i(e, t) {
	return Object.assign(t, e.geocodingQueryParams);
}
function a(e, t) {
	return Object.assign(t, e.reverseQueryParams);
}
//#endregion
//#region src/util.ts
var o = /[&<>"'`]/g, s = /[&<>"'`]/, c = {
	"&": "&amp;",
	"<": "&lt;",
	">": "&gt;",
	"\"": "&quot;",
	"'": "&#x27;",
	"`": "&#x60;"
};
function l(e) {
	return c[e];
}
function u(e) {
	return e == null ? "" : e ? (e = "" + e, s.test(e) ? e.replace(o, l) : e) : e + "";
}
function d(e, t) {
	let n = { Accept: "application/json" }, r = new URL(e);
	return Object.entries(t).forEach(([e, t]) => {
		(Array.isArray(t) ? t : [t]).forEach((t) => {
			r.searchParams.append(e, t);
		});
	}), fetch(r.toString(), { headers: n }).then((e) => e.json());
}
function f(e, t) {
	return e.replace(/\{ *([\w_]+) *\}/g, (e, n) => {
		let r = t[n];
		return r === void 0 ? r = "" : typeof r == "function" && (r = r(t)), u(r);
	});
}
//#endregion
//#region src/geocoders/arcgis.ts
var p = class {
	options = {
		serviceUrl: "https://geocode.arcgis.com/arcgis/rest/services/World/GeocodeServer",
		apiKey: ""
	};
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(t) {
		let n = i(this.options, {
			token: this.options.apiKey,
			SingleLine: t,
			outFields: "Addr_Type",
			forStorage: !1,
			maxLocations: 10,
			f: "json"
		});
		return (await d(this.options.serviceUrl + "/findAddressCandidates", n)).candidates.map((t) => {
			let n = new e.LatLng(t.location.y, t.location.x), r = new e.LatLngBounds(new e.LatLng(t.extent.ymax, t.extent.xmax), new e.LatLng(t.extent.ymin, t.extent.xmin));
			return {
				name: t.address,
				bbox: r,
				center: n
			};
		});
	}
	suggest(e) {
		return this.geocode(e);
	}
	async reverse(t, n) {
		let r = a(this.options, {
			location: t.lng + "," + t.lat,
			distance: 100,
			f: "json"
		}), i = await d(this.options.serviceUrl + "/reverseGeocode", r);
		if (!i || i.error) return [];
		let o = new e.LatLng(i.location.y, i.location.x), s = new e.LatLngBounds(o, o);
		return [{
			name: i.address.Match_addr,
			center: o,
			bbox: s
		}];
	}
};
function ee(e) {
	return new p(e);
}
//#endregion
//#region src/geocoders/bing.ts
var m = class {
	options = { serviceUrl: "https://dev.virtualearth.net/REST/v1/Locations/" };
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(e) {
		let t = i(this.options, {
			query: e,
			key: this.options.apiKey
		}), n = await d(this.options.serviceUrl, t);
		return this._parseResults(n);
	}
	async reverse(e, t) {
		let n = a(this.options, { key: this.options.apiKey }), r = await d(this.options.serviceUrl + e.lat + "," + e.lng, n);
		return this._parseResults(r);
	}
	_parseResults(t) {
		return t.resourceSets[0].resources.map((t) => {
			let n = t.bbox;
			return {
				name: t.name,
				bbox: new e.LatLngBounds([n[0], n[1]], [n[2], n[3]]),
				center: new e.LatLng(...t.point.coordinates)
			};
		});
	}
};
function h(e) {
	return new m(e);
}
//#endregion
//#region src/geocoders/azure.ts
var g = class {
	options = {
		apiKey: "",
		serviceUrl: "https://atlas.microsoft.com/search"
	};
	constructor(t) {
		if (e.Util.setOptions(this, t), !this.options.apiKey) throw Error("Azure Maps Geocoder requires an API key.");
	}
	async geocode(t) {
		let n = {
			"api-version": "1.0",
			query: t,
			"subscription-key": this.options.apiKey
		};
		return ((await d(this.options.serviceUrl + "/address/json", n)).results || []).map((t) => ({
			name: t.address.freeformAddress,
			bbox: new e.LatLngBounds([t.viewport.topLeftPoint.lat, t.viewport.topLeftPoint.lon], [t.viewport.btmRightPoint.lat, t.viewport.btmRightPoint.lon]),
			center: new e.LatLng(t.position.lat, t.position.lon)
		}));
	}
	async reverse(t, n) {
		let r = {
			"api-version": "1.0",
			query: t.lat + "," + t.lng,
			"subscription-key": this.options.apiKey
		};
		return ((await d(this.options.serviceUrl + "/address/reverse/json", r)).addresses || []).map((n) => ({
			name: n.address.freeformAddress,
			bbox: new e.LatLngBounds([n.viewport.topLeftPoint.lat, n.viewport.topLeftPoint.lon], [n.viewport.btmRightPoint.lat, n.viewport.btmRightPoint.lon]),
			center: new e.LatLng(t.lat, t.lng)
		}));
	}
};
function _(e) {
	return new g(e);
}
//#endregion
//#region src/geocoders/google.ts
var v = class {
	options = { serviceUrl: "https://maps.googleapis.com/maps/api/geocode/json" };
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(e) {
		let t = i(this.options, {
			key: this.options.apiKey,
			address: e
		}), n = await d(this.options.serviceUrl, t);
		return this._parseResults(n);
	}
	async reverse(e, t) {
		let n = a(this.options, {
			key: this.options.apiKey,
			latlng: e.lat + "," + e.lng
		}), r = await d(this.options.serviceUrl, n);
		return this._parseResults(r);
	}
	_parseResults(t) {
		return (t.results || [])?.map((t) => {
			let n = new e.LatLng(t.geometry.location.lat, t.geometry.location.lng), r = new e.LatLngBounds(new e.LatLng(t.geometry.viewport.northeast.lat, t.geometry.viewport.northeast.lng), new e.LatLng(t.geometry.viewport.southwest.lat, t.geometry.viewport.southwest.lng));
			return {
				name: t.formatted_address,
				bbox: r,
				center: n,
				properties: t.address_components
			};
		});
	}
};
function y(e) {
	return new v(e);
}
//#endregion
//#region src/geocoders/here.ts
var b = class {
	options = {
		serviceUrl: "https://geocoder.api.here.com/6.2/",
		app_id: "",
		app_code: "",
		apiKey: "",
		maxResults: 5
	};
	constructor(t) {
		if (e.Util.setOptions(this, t), t?.apiKey) throw Error("apiKey is not supported, use app_id/app_code instead!");
	}
	geocode(e) {
		let t = i(this.options, {
			searchtext: e,
			gen: 9,
			app_id: this.options.app_id,
			app_code: this.options.app_code,
			jsonattributes: 1,
			maxresults: this.options.maxResults
		});
		return this.getJSON(this.options.serviceUrl + "geocode.json", t);
	}
	reverse(e, t) {
		let n = e.lat + "," + e.lng;
		this.options.reverseGeocodeProxRadius && (n += "," + this.options.reverseGeocodeProxRadius);
		let r = a(this.options, {
			prox: n,
			mode: "retrieveAddresses",
			app_id: this.options.app_id,
			app_code: this.options.app_code,
			gen: 9,
			jsonattributes: 1,
			maxresults: this.options.maxResults
		});
		return this.getJSON(this.options.serviceUrl + "reversegeocode.json", r);
	}
	async getJSON(t, n) {
		return ((await d(t, n))?.response?.view?.[0]?.result || []).map((t) => {
			let n = t.location, r = new e.LatLng(n.displayPosition.latitude, n.displayPosition.longitude), i = new e.LatLngBounds(new e.LatLng(n.mapView.topLeft.latitude, n.mapView.topLeft.longitude), new e.LatLng(n.mapView.bottomRight.latitude, n.mapView.bottomRight.longitude));
			return {
				name: n.address.label,
				properties: n.address,
				bbox: i,
				center: r
			};
		});
	}
}, x = class {
	options = {
		serviceUrl: "https://geocode.search.hereapi.com/v1",
		apiKey: "",
		app_id: "",
		app_code: "",
		maxResults: 10
	};
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	geocode(e) {
		let t = i(this.options, {
			q: e,
			apiKey: this.options.apiKey,
			limit: this.options.maxResults
		});
		if (!t.at && !t.in) throw Error("at / in parameters not found. Please define coordinates (at=latitude,longitude) or other (in) in your geocodingQueryParams.");
		return this.getJSON(this.options.serviceUrl + "/discover", t);
	}
	reverse(e, t) {
		let n = a(this.options, {
			at: e.lat + "," + e.lng,
			limit: this.options.reverseGeocodeProxRadius,
			apiKey: this.options.apiKey
		});
		return this.getJSON(this.options.serviceUrl + "/revgeocode", n);
	}
	async getJSON(t, n) {
		return ((await d(t, n)).items || []).map((t) => {
			let n = new e.LatLng(t.position.lat, t.position.lng), r;
			return r = t.mapView ? new e.LatLngBounds(new e.LatLng(t.mapView.south, t.mapView.west), new e.LatLng(t.mapView.north, t.mapView.east)) : new e.LatLngBounds(new e.LatLng(t.position.lat, t.position.lng), new e.LatLng(t.position.lat, t.position.lng)), {
				name: t.address.label,
				properties: t.address,
				bbox: r,
				center: n
			};
		});
	}
};
function S(e) {
	return e?.apiKey ? new x(e) : new b(e);
}
//#endregion
//#region src/geocoders/latlng.ts
function C(t) {
	let n;
	if (n = t.match(/^([NS])\s*(\d{1,3}(?:\.\d*)?)\W*([EW])\s*(\d{1,3}(?:\.\d*)?)$/)) return new e.LatLng((/N/i.test(n[1]) ? 1 : -1) * n[2], (/E/i.test(n[3]) ? 1 : -1) * n[4]);
	if (n = t.match(/^(\d{1,3}(?:\.\d*)?)\s*([NS])\W*(\d{1,3}(?:\.\d*)?)\s*([EW])$/)) return new e.LatLng((/N/i.test(n[2]) ? 1 : -1) * n[1], (/E/i.test(n[4]) ? 1 : -1) * n[3]);
	if (n = t.match(/^([NS])\s*(\d{1,3})°?\s*(\d{1,3}(?:\.\d*)?)?['′]?\W*([EW])\s*(\d{1,3})°?\s*(\d{1,3}(?:\.\d*)?)?['′]?$/)) return new e.LatLng((/N/i.test(n[1]) ? 1 : -1) * (+n[2] + n[3] / 60), (/E/i.test(n[4]) ? 1 : -1) * (+n[5] + n[6] / 60));
	if (n = t.match(/^(\d{1,3})°?\s*(\d{1,3}(?:\.\d*)?)?['′]?\s*([NS])\W*(\d{1,3})°?\s*(\d{1,3}(?:\.\d*)?)?['′]?\s*([EW])$/)) return new e.LatLng((/N/i.test(n[3]) ? 1 : -1) * (+n[1] + n[2] / 60), (/E/i.test(n[6]) ? 1 : -1) * (+n[4] + n[5] / 60));
	if (n = t.match(/^([NS])\s*(\d{1,3})°?\s*(\d{1,2})['′]?\s*(\d{1,3}(?:\.\d*)?)?["″]?\W*([EW])\s*(\d{1,3})°?\s*(\d{1,2})['′]?\s*(\d{1,3}(?:\.\d*)?)?["″]?$/)) return new e.LatLng((/N/i.test(n[1]) ? 1 : -1) * (+n[2] + n[3] / 60 + n[4] / 3600), (/E/i.test(n[5]) ? 1 : -1) * (+n[6] + n[7] / 60 + n[8] / 3600));
	if (n = t.match(/^(\d{1,3})°?\s*(\d{1,2})['′]?\s*(\d{1,3}(?:\.\d*)?)?["″]\s*([NS])\W*(\d{1,3})°?\s*(\d{1,2})['′]?\s*(\d{1,3}(?:\.\d*)?)?["″]?\s*([EW])$/)) return new e.LatLng((/N/i.test(n[4]) ? 1 : -1) * (+n[1] + n[2] / 60 + n[3] / 3600), (/E/i.test(n[8]) ? 1 : -1) * (+n[5] + n[6] / 60 + n[7] / 3600));
	if (n = t.match(/^\s*([+-]?\d+(?:\.\d*)?)\s*[\s,]\s*([+-]?\d+(?:\.\d*)?)\s*$/)) return new e.LatLng(+n[1], +n[2]);
}
var w = class {
	options = {
		next: void 0,
		sizeInMeters: 1e4
	};
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(e) {
		let t = C(e);
		return t ? [{
			name: e,
			center: t,
			bbox: t.toBounds(this.options.sizeInMeters)
		}] : this.options.next ? this.options.next.geocode(e) : [];
	}
};
function te(e) {
	return new w(e);
}
//#endregion
//#region src/geocoders/mapbox.ts
var T = class {
	options = { serviceUrl: "https://api.mapbox.com/geocoding/v5/mapbox.places/" };
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	_getProperties(e) {
		let t = {
			text: e.text,
			address: e.address
		};
		return (e.context || []).forEach((e) => {
			let n = e.id.split(".")[0];
			t[n] = e.text, e.short_code && (t.countryShortCode = e.short_code);
		}), t;
	}
	async geocode(e) {
		let t = this.options.serviceUrl + encodeURIComponent(e) + ".json", n = i(this.options, { access_token: this.options.apiKey });
		n.proximity !== void 0 && n.proximity.lat !== void 0 && n.proximity.lng !== void 0 && (n.proximity = n.proximity.lng + "," + n.proximity.lat);
		let r = await d(t, n);
		return this._parseResults(r);
	}
	suggest(e) {
		return this.geocode(e);
	}
	async reverse(e, t) {
		let n = await d(this.options.serviceUrl + e.lng + "," + e.lat + ".json", a(this.options, { access_token: this.options.apiKey }));
		return this._parseResults(n);
	}
	_parseResults(t) {
		return t.features?.length ? t.features.map((t) => {
			let n = new e.LatLng(...t.center.reverse()), r;
			return r = t.bbox ? new e.LatLngBounds(new e.LatLng(...t.bbox.slice(0, 2).reverse()), new e.LatLng(...t.bbox.slice(2, 4).reverse())) : new e.LatLngBounds(n, n), {
				name: t.place_name,
				bbox: r,
				center: n,
				properties: this._getProperties(t)
			};
		}) : [];
	}
};
function E(e) {
	return new T(e);
}
//#endregion
//#region src/geocoders/mapquest.ts
var D = class {
	options = { serviceUrl: "https://www.mapquestapi.com/geocoding/v1" };
	constructor(t) {
		e.Util.setOptions(this, t), this.options.apiKey = decodeURIComponent(this.options.apiKey);
	}
	_formatName(...e) {
		return e.filter((e) => !!e).join(", ");
	}
	async geocode(e) {
		let t = i(this.options, {
			key: this.options.apiKey,
			location: e,
			limit: 5,
			outFormat: "json"
		}), n = await d(this.options.serviceUrl + "/address", t);
		return this._parseResults(n);
	}
	async reverse(e, t) {
		let n = a(this.options, {
			key: this.options.apiKey,
			location: e.lat + "," + e.lng,
			outputFormat: "json"
		}), r = await d(this.options.serviceUrl + "/reverse", n);
		return this._parseResults(r);
	}
	_parseResults(t) {
		return (t.results?.[0]?.locations || []).map((t) => {
			let n = new e.LatLng(t.latLng.lat, t.latLng.lng);
			return {
				name: this._formatName(t.street, t.adminArea4, t.adminArea3, t.adminArea1),
				bbox: new e.LatLngBounds(n, n),
				center: n
			};
		});
	}
};
function O(e) {
	return new D(e);
}
//#endregion
//#region src/geocoders/neutrino.ts
var k = class {
	options = {
		userId: "",
		apiKey: "",
		serviceUrl: "https://neutrinoapi.com/"
	};
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(t) {
		let n = i(this.options, {
			apiKey: this.options.apiKey,
			userId: this.options.userId,
			address: t.split(/\s+/).join(".")
		}), r = await d(this.options.serviceUrl + "geocode-address", n);
		if (!r.locations) return [];
		r.geometry = r.locations[0];
		let a = new e.LatLng(r.geometry.latitude, r.geometry.longitude), o = new e.LatLngBounds(a, a);
		return [{
			name: r.geometry.address,
			bbox: o,
			center: a
		}];
	}
	suggest(e) {
		return this.geocode(e);
	}
	async reverse(t, n) {
		let r = a(this.options, {
			apiKey: this.options.apiKey,
			userId: this.options.userId,
			latitude: t.lat,
			longitude: t.lng
		}), i = await d(this.options.serviceUrl + "geocode-reverse", r);
		if (i.status.status !== 200 || !i.found) return [];
		let o = new e.LatLng(t.lat, t.lng), s = new e.LatLngBounds(o, o);
		return [{
			name: i.address,
			bbox: s,
			center: o
		}];
	}
};
function A(e) {
	return new k(e);
}
//#endregion
//#region src/geocoders/nominatim.ts
var ne = 1e3, j = "https://nominatim.openstreetmap.org/", M = class {
	options = {
		serviceUrl: j,
		htmlTemplate(e) {
			let t = e.address, n, r = [];
			return (t.road || t.building) && r.push("{building} {road} {house_number}"), (t.city || t.town || t.village || t.hamlet) && (n = r.length > 0 ? "leaflet-control-geocoder-address-detail" : "", r.push("<span class=\"" + n + "\">{postcode} {city} {town} {village} {hamlet}</span>")), (t.state || t.country) && (n = r.length > 0 ? "leaflet-control-geocoder-address-context" : "", r.push("<span class=\"" + n + "\">{state} {country}</span>")), f(r.join("<br/>"), t);
		}
	};
	_lastRequestStart = Promise.resolve(0);
	_cache = /* @__PURE__ */ new Map();
	constructor(t) {
		e.Util.setOptions(this, t || {});
	}
	_getJSON(e, t) {
		let n = this.options.serviceUrl + e, r = n + JSON.stringify(t), i = this._cache.get(r);
		return i || (i = this._rateLimit().then(() => d(n, t)), i.catch(() => this._cache.delete(r)), this._cache.set(r, i)), i;
	}
	_rateLimit() {
		let { hostname: e } = new URL(this.options.serviceUrl);
		if (e !== new URL(j).hostname) return Promise.resolve();
		let t = this._lastRequestStart.then(async (e) => {
			let t = ne - (Date.now() - e);
			return t > 0 && await new Promise((e) => setTimeout(e, t)), Date.now();
		});
		return this._lastRequestStart = t, t;
	}
	async geocode(t) {
		let n = i(this.options, {
			q: t,
			limit: 5,
			format: "json",
			addressdetails: 1
		});
		return (await this._getJSON("search", n)).map((t) => {
			let n = t.boundingbox;
			return {
				icon: t.icon,
				name: t.display_name,
				html: this.options.htmlTemplate ? this.options.htmlTemplate(t) : void 0,
				bbox: new e.LatLngBounds([+n[0], +n[2]], [+n[1], +n[3]]),
				center: new e.LatLng(+t.lat, +t.lon),
				properties: t
			};
		});
	}
	async suggest(e) {
		throw new r("Nominatim forbids auto-complete search: \"you must not implement such a service on the client side using the API\". See https://operations.osmfoundation.org/policies/nominatim/");
	}
	async reverse(t, n) {
		let r = a(this.options, {
			lat: t.lat,
			lon: t.lng,
			zoom: Math.round(Math.log(n / 256) / Math.log(2)),
			addressdetails: 1,
			format: "json"
		}), i = await this._getJSON("reverse", r);
		if (!i?.lat || !i?.lon) return [];
		let o = new e.LatLng(+i.lat, +i.lon), s = new e.LatLngBounds(o, o);
		return [{
			name: i.display_name,
			html: this.options.htmlTemplate ? this.options.htmlTemplate(i) : void 0,
			center: o,
			bbox: s,
			properties: i
		}];
	}
};
function N(e) {
	return new M(e);
}
//#endregion
//#region src/geocoders/open-location-code.ts
var P = class {
	options = {};
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(t) {
		try {
			let n = this.options.OpenLocationCode.decode(t);
			return [{
				name: t,
				center: new e.LatLng(n.latitudeCenter, n.longitudeCenter),
				bbox: new e.LatLngBounds(new e.LatLng(n.latitudeLo, n.longitudeLo), new e.LatLng(n.latitudeHi, n.longitudeHi))
			}];
		} catch (e) {
			return console.warn(e), [];
		}
	}
	async reverse(t, n) {
		try {
			return [{
				name: this.options.OpenLocationCode.encode(t.lat, t.lng, this.options.codeLength),
				center: new e.LatLng(t.lat, t.lng),
				bbox: new e.LatLngBounds(new e.LatLng(t.lat, t.lng), new e.LatLng(t.lat, t.lng))
			}];
		} catch (e) {
			return console.warn(e), [];
		}
	}
};
function F(e) {
	return new P(e);
}
//#endregion
//#region src/geocoders/opencage.ts
var I = class {
	options = { serviceUrl: "https://api.opencagedata.com/geocode/v1/json" };
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(e) {
		let t = i(this.options, {
			key: this.options.apiKey,
			q: e
		}), n = await d(this.options.serviceUrl, t);
		return this._parseResults(n);
	}
	suggest(e) {
		return this.geocode(e);
	}
	async reverse(e, t) {
		let n = a(this.options, {
			key: this.options.apiKey,
			q: [e.lat, e.lng].join(",")
		}), r = await d(this.options.serviceUrl, n);
		return this._parseResults(r);
	}
	_parseResults(t) {
		return (t.results || []).map((t) => {
			let n = new e.LatLng(t.geometry.lat, t.geometry.lng), r = t.annotations && t.annotations.bounds ? new e.LatLngBounds(new e.LatLng(t.annotations.bounds.northeast.lat, t.annotations.bounds.northeast.lng), new e.LatLng(t.annotations.bounds.southwest.lat, t.annotations.bounds.southwest.lng)) : new e.LatLngBounds(n, n);
			return {
				name: t.formatted,
				bbox: r,
				center: n,
				properties: t
			};
		});
	}
};
function L(e) {
	return new I(e);
}
//#endregion
//#region src/geocoders/pelias.ts
var R = class {
	options = { serviceUrl: "https://api.geocode.earth/v1" };
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(e) {
		let t = i(this.options, {
			api_key: this.options.apiKey,
			text: e
		}), n = await d(this.options.serviceUrl + "/search", t);
		return this._parseResults(n);
	}
	async suggest(e) {
		let t = i(this.options, {
			api_key: this.options.apiKey,
			text: e
		}), n = await d(this.options.serviceUrl + "/autocomplete", t);
		return this._parseResults(n);
	}
	async reverse(e, t) {
		let n = a(this.options, {
			api_key: this.options.apiKey,
			"point.lat": e.lat,
			"point.lon": e.lng
		}), r = await d(this.options.serviceUrl + "/reverse", n);
		return this._parseResults(r);
	}
	_parseResults(t) {
		return (t.features || []).map((t) => {
			let n = t.geometry.coordinates, r = new e.LatLng(n[1], n[0]), i = Array.isArray(t.bbox) && t.bbox.length === 4 ? new e.LatLngBounds([t.bbox[1], t.bbox[0]], [t.bbox[3], t.bbox[2]]) : new e.LatLngBounds(r, r);
			return {
				name: t.properties.label,
				center: r,
				bbox: i,
				properties: t.properties
			};
		});
	}
};
function z(e) {
	return new R(e);
}
var B = R, V = z, H = R, U = z, W = class extends R {
	constructor(e) {
		super(Object.assign({ serviceUrl: "https://api.openrouteservice.org/geocode" }, e));
	}
};
function G(e) {
	return new W(e);
}
//#endregion
//#region src/geocoders/photon.ts
var K = class {
	options = {
		serviceUrl: "https://photon.komoot.io/api/",
		reverseUrl: "https://photon.komoot.io/reverse/",
		nameProperties: [
			"name",
			"street",
			"suburb",
			"hamlet",
			"town",
			"city",
			"state",
			"country"
		]
	};
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(e, t) {
		let n = i(this.options, { q: e }), r = t?.map?.getCenter?.();
		r && (n.lat = r.lat, n.lon = r.lng);
		let a = t?.map?.getZoom?.();
		a && (n.zoom = a);
		let o = await d(this.options.serviceUrl, n);
		return this._parseResults(o);
	}
	suggest(e) {
		return this.geocode(e);
	}
	async reverse(e, t) {
		let n = a(this.options, {
			lat: e.lat,
			lon: e.lng
		}), r = await d(this.options.reverseUrl, n);
		return this._parseResults(r);
	}
	_parseResults(t) {
		return (t.features || []).map((t) => {
			let n = t.geometry.coordinates, r = new e.LatLng(n[1], n[0]), i = t.properties?.extent, a = i ? new e.LatLngBounds([i[1], i[0]], [i[3], i[2]]) : new e.LatLngBounds(r, r);
			return {
				name: this._decodeFeatureName(t),
				html: this.options.htmlTemplate ? this.options.htmlTemplate(t) : void 0,
				center: r,
				bbox: a,
				properties: t.properties
			};
		});
	}
	_decodeFeatureName(e) {
		return (this.options.nameProperties || []).map((t) => e.properties?.[t]).filter((e) => !!e).join(", ");
	}
};
function q(e) {
	return new K(e);
}
//#endregion
//#region src/geocoders/what3words.ts
var J = class {
	options = { serviceUrl: "https://api.what3words.com/v2/" };
	constructor(t) {
		e.Util.setOptions(this, t);
	}
	async geocode(t) {
		let n = await d(this.options.serviceUrl + "forward", i(this.options, {
			key: this.options.apiKey,
			addr: t.split(/\s+/).join(".")
		}));
		if (!n.geometry) return [];
		let r = new e.LatLng(n.geometry.lat, n.geometry.lng), a = new e.LatLngBounds(r, r);
		return [{
			name: n.words,
			bbox: a,
			center: r
		}];
	}
	suggest(e) {
		return this.geocode(e);
	}
	async reverse(t, n) {
		let r = await d(this.options.serviceUrl + "reverse", a(this.options, {
			key: this.options.apiKey,
			coords: [t.lat, t.lng].join(",")
		}));
		if (r.status.status != 200) return [];
		let i = new e.LatLng(r.geometry.lat, r.geometry.lng), o = new e.LatLngBounds(i, i);
		return [{
			name: r.words,
			bbox: o,
			center: i
		}];
	}
};
function Y(e) {
	return new J(e);
}
//#endregion
//#region src/geocoders/index.ts
var X = /* @__PURE__ */ n({
	ArcGis: () => p,
	AzureMaps: () => g,
	Bing: () => m,
	GeocodeEarth: () => B,
	Google: () => v,
	HERE: () => b,
	HEREv2: () => x,
	LatLng: () => w,
	MapQuest: () => D,
	Mapbox: () => T,
	Mapzen: () => H,
	Neutrino: () => k,
	Nominatim: () => M,
	OpenCage: () => I,
	OpenLocationCode: () => P,
	Openrouteservice: () => W,
	Pelias: () => R,
	Photon: () => K,
	SuggestUnsupportedError: () => r,
	What3Words: () => J,
	arcgis: () => ee,
	azure: () => _,
	bing: () => h,
	geocodeEarth: () => V,
	geocodingParams: () => i,
	google: () => y,
	here: () => S,
	latLng: () => te,
	mapQuest: () => O,
	mapbox: () => E,
	mapzen: () => U,
	neutrino: () => A,
	nominatim: () => N,
	openLocationCode: () => F,
	opencage: () => L,
	openrouteservice: () => G,
	parseLatLng: () => C,
	pelias: () => z,
	photon: () => q,
	reverseParams: () => a,
	what3words: () => Y
}), Z = class {
	constructor(...e) {}
};
Object.getOwnPropertyNames(e.Control.prototype).forEach((t) => Z.prototype[t] = e.Control.prototype[t]), Object.getOwnPropertyNames(e.Evented.prototype).forEach((t) => Z.prototype[t] = e.Evented.prototype[t]);
var Q = class extends Z {
	options = {
		showUniqueResult: !0,
		showResultIcons: !1,
		collapsed: !0,
		expand: "touch",
		position: "topright",
		placeholder: "Search...",
		errorMessage: "Nothing found.",
		iconLabel: "Initiate a new search",
		query: "",
		queryMinLength: 1,
		suggestMinLength: 3,
		suggestTimeout: 250,
		defaultMarkGeocode: !0
	};
	_alts;
	_container;
	_errorElement;
	_geocodeMarker;
	_input;
	_lastGeocode;
	_map;
	_preventBlurCollapse;
	_requestCount = 0;
	_results;
	_selection;
	_suggestTimeout;
	_suggestUnsupported = !1;
	constructor(t) {
		super(t), e.Util.setOptions(this, t), this.options.geocoder || (this.options.geocoder = new M());
	}
	addThrobberClass() {
		this._container.classList.add("leaflet-control-geocoder-throbber");
	}
	removeThrobberClass() {
		this._container.classList.remove("leaflet-control-geocoder-throbber");
	}
	onAdd(t) {
		let n = e.DomUtil.create("div", "leaflet-control-geocoder leaflet-bar"), r = e.DomUtil.create("button", "leaflet-control-geocoder-icon", n), i = e.DomUtil.create("div", "leaflet-control-geocoder-form", n);
		this._map = t, this._container = n, r.innerHTML = "&nbsp;", r.type = "button", r.setAttribute("aria-label", this.options.iconLabel);
		let a = this._input = e.DomUtil.create("input", "", i);
		return a.type = "search", a.value = this.options.query, a.placeholder = this.options.placeholder, e.DomEvent.disableClickPropagation(a), this._errorElement = e.DomUtil.create("div", "leaflet-control-geocoder-form-no-error", n), this._errorElement.innerHTML = this.options.errorMessage, this._alts = e.DomUtil.create("ul", "leaflet-control-geocoder-alternatives leaflet-control-geocoder-alternatives-minimized", n), e.DomEvent.disableClickPropagation(this._alts), e.DomEvent.on(a, "keydown", this._keydown, this), this.options.geocoder?.suggest && e.DomEvent.on(a, "input", this._change, this), e.DomEvent.on(a, "blur", () => {
			this.options.collapsed && !this._preventBlurCollapse && this._collapse(), this._preventBlurCollapse = !1;
		}), this.options.collapsed ? this.options.expand === "click" ? e.DomEvent.on(n, "click", (e) => {
			e.button === 0 && e.detail !== 2 && this._toggle();
		}) : this.options.expand === "touch" ? e.DomEvent.on(n, e.Browser.touch ? "touchstart mousedown" : "mousedown", (e) => {
			this._toggle(), e.preventDefault(), e.stopPropagation();
		}, this) : (e.DomEvent.on(n, "mouseover", this._expand, this), e.DomEvent.on(n, "mouseout", this._collapse, this), this._map.on("movestart", this._collapse, this)) : (this._expand(), e.Browser.touch ? e.DomEvent.on(n, "touchstart", () => this._geocode()) : e.DomEvent.on(n, "click", () => this._geocode())), this.options.defaultMarkGeocode && this.on("markgeocode", this.markGeocode, this), this.on("startgeocode", this.addThrobberClass, this), this.on("finishgeocode", this.removeThrobberClass, this), this.on("startsuggest", this.addThrobberClass, this), this.on("finishsuggest", this.removeThrobberClass, this), e.DomEvent.disableClickPropagation(n), n;
	}
	setQuery(e) {
		return this._input.value = e, this;
	}
	_geocodeResult(e, t) {
		!t && this.options.showUniqueResult && e.length === 1 ? this._geocodeResultSelected(e[0]) : e.length > 0 ? (this._alts.innerHTML = "", this._results = e, this._alts.classList.remove("leaflet-control-geocoder-alternatives-minimized"), this._container.classList.add("leaflet-control-geocoder-options-open"), this._results.forEach((e, t) => this._alts.appendChild(this._createAlt(e, t)))) : (this._container.classList.add("leaflet-control-geocoder-options-error"), this._errorElement.classList.add("leaflet-control-geocoder-error"));
	}
	markGeocode(t) {
		let n = t.geocode;
		return this._map.fitBounds(n.bbox), this._geocodeMarker && this._map.removeLayer(this._geocodeMarker), this._geocodeMarker = new e.Marker(n.center).bindPopup(n.html || n.name).addTo(this._map).openPopup(), this;
	}
	async _geocode(t = !1) {
		let n = this._input.value;
		if (!t && n.length < this.options.queryMinLength) return;
		let i = ++this._requestCount;
		this._lastGeocode = n, t || this._clearResults();
		let a = { input: n };
		this.fire(t ? "startsuggest" : "startgeocode", a);
		let o = { map: this._map }, s;
		if (t) try {
			s = await this.options.geocoder.suggest(n, o);
		} catch (t) {
			if (t instanceof r) {
				this._suggestUnsupported = !0, e.DomEvent.off(this._input, "input", this._change, this);
				return;
			}
			throw t;
		}
		else s = await this.options.geocoder.geocode(n, o);
		if (i === this._requestCount) {
			let e = {
				input: n,
				results: s
			};
			this.fire(t ? "finishsuggest" : "finishgeocode", e), this._geocodeResult(s, t);
		}
	}
	_geocodeResultSelected(e) {
		let t = { geocode: e };
		this.fire("markgeocode", t);
	}
	_toggle() {
		this._container.classList.contains("leaflet-control-geocoder-expanded") ? this._collapse() : this._expand();
	}
	_expand() {
		this._container.classList.add("leaflet-control-geocoder-expanded"), this._input.select(), this.fire("expand");
	}
	_collapse() {
		this._container.classList.remove("leaflet-control-geocoder-expanded"), this._alts.classList.add("leaflet-control-geocoder-alternatives-minimized"), this._errorElement.classList.remove("leaflet-control-geocoder-error"), this._container.classList.remove("leaflet-control-geocoder-options-open"), this._container.classList.remove("leaflet-control-geocoder-options-error"), this._input.blur(), this.fire("collapse");
	}
	_clearResults() {
		this._alts.classList.add("leaflet-control-geocoder-alternatives-minimized"), this._selection = null, this._errorElement.classList.remove("leaflet-control-geocoder-error"), this._container.classList.remove("leaflet-control-geocoder-options-open"), this._container.classList.remove("leaflet-control-geocoder-options-error");
	}
	_createAlt(t, n) {
		let r = e.DomUtil.create("li", ""), i = e.DomUtil.create("a", "", r), a = this.options.showResultIcons && t.icon ? e.DomUtil.create("img", "", i) : null, o = t.html ? void 0 : document.createTextNode(t.name);
		return a && (a.src = t.icon), r.setAttribute("data-result-index", String(n)), t.html ? i.innerHTML += t.html : o && i.appendChild(o), e.DomEvent.on(r, "mousedown touchstart", (n) => {
			this._preventBlurCollapse = !0, e.DomEvent.stop(n), this._geocodeResultSelected(t), e.DomEvent.on(r, "click touchend", () => {
				this.options.collapsed ? this._collapse() : this._clearResults();
			});
		}, this), r;
	}
	_keydown(t) {
		let n = (e) => {
			this._selection &&= (this._selection.classList.remove("leaflet-control-geocoder-selected"), this._selection[e > 0 ? "nextSibling" : "previousSibling"]), this._selection ||= this._alts[e > 0 ? "firstChild" : "lastChild"], this._selection && this._selection.classList.add("leaflet-control-geocoder-selected");
		};
		switch (t.key) {
			case "Escape":
				this.options.collapsed ? this._collapse() : this._clearResults();
				break;
			case "ArrowUp":
				n(-1);
				break;
			case "ArrowDown":
				n(1);
				break;
			case "Enter":
				if (this._selection) {
					let e = parseInt(this._selection.getAttribute("data-result-index"), 10);
					this._geocodeResultSelected(this._results[e]), this._clearResults();
				} else this._geocode();
				break;
			default: return;
		}
		e.DomEvent.preventDefault(t);
	}
	_change() {
		if (this._suggestUnsupported) return;
		let e = this._input.value;
		e !== this._lastGeocode && (clearTimeout(this._suggestTimeout), e.length >= this.options.suggestMinLength ? this._suggestTimeout = setTimeout(() => this._geocode(!0), this.options.suggestTimeout) : this._clearResults());
	}
};
function $(e) {
	return new Q(e);
}
//#endregion
//#region src/index.ts
Object.assign(Q, X);
var re = Q;
Object.assign(e.Control, {
	Geocoder: Q,
	geocoder: $
});
//#endregion
export { Q as Geocoder, re as default, $ as geocoder, X as geocoders };

//# sourceMappingURL=Control.Geocoder.modern.js.map